# This value is overridden at Docker build time by the VITE_API_URL build arg.
# See .github/workflows/aws-deploy.yml and AWS_SETUP.md.
# Update this only for local production builds (npm run build outside Docker).
# After creating your Elastic Beanstalk environment, replace the URL below.
VITE_API_URL=https://saria-portal-env.me-central-1.elasticbeanstalk.com/api
