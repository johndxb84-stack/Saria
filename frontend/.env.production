# This value is overridden at Docker build time by the VITE_API_URL build arg.
# See .github/workflows/azure-deploy.yml and AZURE_SETUP.md.
# Update this only for local production builds (npm run build outside Docker).
# After creating your Azure App Service, replace the URL below.
VITE_API_URL=https://saria-portal.azurewebsites.net/api
